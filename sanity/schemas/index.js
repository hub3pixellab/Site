import lead from './lead';
import matchmaker from './matchmaker';
import project from './project';
import whitepaper from './whitepaper';

export const schemaTypes = [lead, matchmaker, project, whitepaper];
export default schemaTypes;
